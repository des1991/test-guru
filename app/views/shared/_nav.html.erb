<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
  <ul class="navbar-nav mr-auto">
    <li class="nav-item"><%= link_to t('nav.tests'), tests_path, class: 'nav-link' %></li>
    <li class="nav-item"><%= link_to t('nav.feedback'), new_feedback_path, class: 'nav-link' %></li>
    <li class="nav-item"><%= link_to t('nav.badges'), badges_path, class: 'nav-link' %></li>
  </ul>  
  <span class="navbar-text">
    <% if user_signed_in? %>
      <%= t('.welcome_user', user: current_user.email) %>      
      <% if current_user.admin? %>
        <%= link_to t('.admin_link'), admin_tests_path, class: "btn btn-primary text-white" %>
      <% end %> |
      <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>  
    <% else %>
      <%= link_to t('.login'), new_user_session_path %> |
      <%= link_to t('.signup'), new_user_registration_path %>
    <% end %>
  </span>
</nav>
